# HushWake 项目指南

## 项目目的

本仓库用于独立推进 HushWake（中文名：悄醒）Android App 的产品设计、技术验证、开发、测试与发布。

## 不可破坏的核心约束

- 闹钟与白噪音默认使用智能输出：会话开始时无耳机则允许系统媒体外放；检测到耳机则必须进入耳机路由守卫。
- 耳机模式无法验证实际输出路径时必须保持静音，不得自动降级到手机扬声器、听筒、投屏或车机。
- 耳机断连或路由发生不确定变化时，处理顺序必须是先将应用播放器增益归零，再停止播放并执行用户允许的振动或通知兜底。
- 不得把 Android 的首选输出设备等同于已经验证的实际唯一输出设备。
- 默认采用本地数据；未经用户独立同意，不上传精确闹钟时间、标签、蓝牙名称或稳定硬件标识。
- 不得宣称产品能够治疗失眠、提供医疗效果或在关机、无电、强制停止等条件下保证唤醒。

## 事实来源与任务路由

- 产品范围、流程、状态、交互、异常和待决事项以 `docs/hush-wake-prd.md` 为当前事实来源。
- 改变用户工作流、兼容等级、隐私承诺或数据策略时，同步更新 PRD。
- Android 工程建立后，将构建、架构和测试细节写入代码附近的文档，不堆入本文件。

## 当前开发门禁

- `0.3.4-beta` 是当前正式功能测试版，最低 Android 版本为 API 31；仍不得描述为公开稳定版。
- API 31–35 仅在当前耳机完成低增益人工确认、设备级本地验证仍有效且本次实际路由通过后开放兼容有声；API 36+ 使用全路由强验证。
- 按 `docs/device-test-guide.md` 完成智能外放、耳机闹钟、耳机白噪音、接入/断连竞态和耳机模式零扬声器串音验收，才可解除 beta 发布门禁。
- 验证不得通过减少耳机路径检查、隐藏阻断结果或在耳机会话内启用扬声器降级来通过。

## 最小验证入口

- 状态机单元测试：`.\gradlew.bat :app:testDebugUnitTest`。
- Android 静态检查与测试包：`.\gradlew.bat :app:lintDebug :app:assembleDebug`。
- 闹铃素材连续性：`.\scripts\verify-alarm-audio.ps1`（需要 `ffmpeg`/`ffprobe`）。
- 设备启动回归：`scripts/verify-app-launch.ps1`（需要 ADB 已连接且已有测试 APK）。
- 核心安全结论必须补充实体机证据；本地构建通过不能替代 `docs/device-test-guide.md` 的测试。
